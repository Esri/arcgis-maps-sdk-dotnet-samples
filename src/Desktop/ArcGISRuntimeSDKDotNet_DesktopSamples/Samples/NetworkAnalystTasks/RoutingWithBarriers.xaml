<UserControl x:Class="ArcGISRuntimeSDKDotNet_DesktopSamples.Samples.RoutingWithBarriers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013">
    <Grid>
        <Grid.Resources>
            <esri:SimpleLineSymbol x:Key="RouteSymbol" Color="#990000FF" Style="Solid" Width="3" />
            <esri:SimpleRenderer x:Key="RouteLayerRenderer" Symbol="{StaticResource RouteSymbol}" />

            <esri:SimpleMarkerSymbol x:Key="BarrierSymbol" Color="Black" Style="X" Size="20" />
            <esri:SimpleRenderer x:Key="BarrierLayerRenderer" Symbol="{StaticResource BarrierSymbol}" />
        </Grid.Resources>

        <esri:MapView x:Name="mapView" MapViewTapped="mapView_MapViewTapped">
			<esri:Map InitialViewpoint="-13637527,4545455,-13622931,4552890,3857">
                <esri:ArcGISTiledMapServiceLayer
                    ServiceUri="http://services.arcgisonline.com/ArcGIS/rest/services/World_Street_Map/MapServer" />

                <esri:GraphicsLayer ID="RouteGraphicsLayer" Renderer="{StaticResource RouteLayerRenderer}" />
                <esri:GraphicsLayer ID="StopGraphicsLayer" />
                <esri:GraphicsLayer ID="BarrierGraphicsLayer" Renderer="{StaticResource BarrierLayerRenderer}" />
            </esri:Map>
        </esri:MapView>

        <Border Background="White" BorderBrush="Black" BorderThickness="1" Width="250"
                HorizontalAlignment="Right" VerticalAlignment="Top" Margin="30" Padding="20">
            <StackPanel>
                <TextBlock Text="Tap stop and barrier points on the map to see the calculated route." 
                           TextWrapping="Wrap" />

                <StackPanel Orientation="Horizontal" Margin="0,12,0,0" HorizontalAlignment="Center">
                    <RadioButton x:Name="rbStops" Content="Add Stops" Margin="0,0,20,0" IsChecked="True" />
                    <RadioButton x:Name="rbBarriers" Content="Add Barriers" />
                </StackPanel>

                <Button Content="Clear" Click="ClearButton_Click" HorizontalAlignment="Center" Padding="20,4" Margin="0,12,0,0"/>

                <ProgressBar x:Name="progress" IsIndeterminate="True" Visibility="Collapsed" Margin="0,12,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
