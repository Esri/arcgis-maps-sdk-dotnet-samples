<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage
    x:Class="ArcGISRuntime.SampleSettingsPage "
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:ArcGISRuntimeXamarin.Converters;assembly=ArcGISRuntime"
    xmlns:converters1="clr-namespace:Forms.Converters"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:Forms.Resources;assembly=ArcGISRuntime"
    mc:Ignorable="d">
    <TabbedPage.Children>
        <ContentPage Title="About">
            <ContentPage.Content>
                <WebView x:Name="AboutPage" />
            </ContentPage.Content>
        </ContentPage>
        <ContentPage Title="Licenses">
            <ContentPage.Content>
                <WebView x:Name="LicensePage" />
            </ContentPage.Content>
        </ContentPage>
        <ContentPage Title="Offline data">
            <ContentPage.Content>
                <StackLayout Orientation="Vertical">
                    <StackLayout Orientation="Horizontal">
                        <Button Clicked="DownloadAllClicked" Text="Download all" />
                        <Button Clicked="DeleteAllClicked" Text="Delete all" />
                    </StackLayout>
                    <ListView x:Name="OfflineDataView">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <Grid x:Name="ListViewGrid">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Label Grid.Column="0" Text="{Binding SampleName}" />
                                        <ImageButton
                                            Grid.Column="1"
                                            Clicked="DownloadClicked"
                                            CommandParameter="{Binding .}"
                                            MinimumHeightRequest="25"
                                            MinimumWidthRequest="25"
                                            Source="{resources:ImageResource ArcGISRuntime.Resources.download.png}" />
                                        <ImageButton
                                            Grid.Column="2"
                                            Clicked="AGOLClicked"
                                            CommandParameter="{Binding .}"
                                            MinimumHeightRequest="25"
                                            MinimumWidthRequest="25"
                                            Source="{resources:ImageResource ArcGISRuntime.Resources.agol.png}" />
                                        <ImageButton
                                            Grid.Column="3"
                                            Clicked="DeleteClicked"
                                            CommandParameter="{Binding .}"
                                            MinimumHeightRequest="25"
                                            MinimumWidthRequest="25"
                                            Source="{resources:ImageResource ArcGISRuntime.Resources.delete.png}" />
                                    </Grid>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <Label x:Name="StatusLabel" />
                </StackLayout>
            </ContentPage.Content>
        </ContentPage>
    </TabbedPage.Children>
</TabbedPage>