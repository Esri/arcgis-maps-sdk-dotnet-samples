<ContentPage
    x:Class="ArcGISRuntimeXamarin.Samples.NavigateRoute.NavigateRoute"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:esriUI="clr-namespace:Esri.ArcGISRuntime.Xamarin.Forms;assembly=Esri.ArcGISRuntime.Xamarin.Forms">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <esriUI:MapView
            x:Name="MyMapView"
            Grid.Row="0"
            Grid.RowSpan="2" />
        <Grid Grid.Row="0" BackgroundColor="#AA000000">
            <Label
                x:Name="MessagesTextBlock"
                Margin="5"
                Text=""
                TextColor="White" />
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button
                x:Name="StartNavigationButton"
                Grid.Column="0"
                Clicked="StartNavigation"
                Text="Navigate" />
            <Button
                x:Name="RecenterButton"
                Grid.Column="1"
                Clicked="RecenterButton_Click"
                IsEnabled="False"
                Text="Recenter" />
        </Grid>
    </Grid>
</ContentPage>
