<ContentPage
    x:Class="ArcGISRuntimeXamarin.Samples.ConfigureSubnetworkTrace.ConfigureSubnetworkTrace"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:esriUI="clr-namespace:Esri.ArcGISRuntime.Xamarin.Forms;assembly=Esri.ArcGISRuntime.Xamarin.Forms"
    xmlns:mapping="clr-namespace:Esri.ArcGISRuntime.Mapping;assembly=Esri.ArcGISRuntime">
    <Grid>
        <TableView
            x:Name="ConfigureTable"
            HasUnevenRows="True"
            Intent="Settings">
            <TableRoot>
                <TableSection Title="Trace options">
                    <SwitchCell
                        On="True"
                        OnChanged="IncludeBarriersChanged"
                        Text="Include barriers" />
                    <SwitchCell
                        On="True"
                        OnChanged="IncludContainersChanged"
                        Text="Include containers" />
                    <!--  NOTE that There are also: IncludeContent, IncludeStructures, IgnoreBarriersAtStartingPoints, ValidateConsistency  -->
                </TableSection>
                <TableSection Title="Define new condition">
                    <ViewCell>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="auto" />
                            </Grid.RowDefinitions>
                            <Picker
                                x:Name="ComparisonSources"
                                Grid.Row="0"
                                ItemDisplayBinding="{Binding Name}"
                                SelectedIndexChanged="OnComparisonSourceChanged" />
                            <Picker x:Name="ComparisonOperators" Grid.Row="1" />
                            <Picker
                                x:Name="ComparisonValueChoices"
                                Grid.Row="2"
                                ItemDisplayBinding="{Binding Name}" />
                            <Entry
                                x:Name="ComparisonValue"
                                Grid.Row="2"
                                IsVisible="False" />
                            <Button
                                Grid.Row="3"
                                Clicked="OnAddCondition"
                                Text="Add condition" />
                        </Grid>
                    </ViewCell>
                </TableSection>
                <TableSection Title="Barrier conditions">
                    <ViewCell>
                        <Label
                            x:Name="ConditionBarrierExpression"
                            FontSize="Large"
                            Text="" />
                    </ViewCell>
                </TableSection>
                <TableSection>
                    <ViewCell>
                        <StackLayout Orientation="Vertical">
                            <Button Clicked="OnTrace" Text="Trace" />
                            <Button Clicked="OnReset" Text="Reset" />
                        </StackLayout>
                    </ViewCell>
                </TableSection>
            </TableRoot>
        </TableView>
    </Grid>
</ContentPage>
