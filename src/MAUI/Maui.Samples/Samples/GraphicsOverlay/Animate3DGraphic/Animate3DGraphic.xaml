<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="ArcGIS.Samples.Animate3DGraphic.Animate3DGraphic"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:esriUI="clr-namespace:Esri.ArcGISRuntime.Maui;assembly=Esri.ArcGISRuntime.Maui">
    <Grid Style="{DynamicResource EsriSampleContainer}">
        <esriUI:SceneView x:Name="MySceneView" AtmosphereEffect="Realistic" Style="{DynamicResource EsriSampleGeoView}" />
        <Border WidthRequest="{OnPlatform MacCatalyst=375, WinUI=375}"
                Style="{DynamicResource EsriSampleControlPanel}">
            <StackLayout>
                <StackLayout Orientation="Horizontal" Spacing="5">
                    <Picker x:Name="MissionSelectionBox" WidthRequest="125" HorizontalTextAlignment="Center" />
                    <Button x:Name="MissionPlayPause"
                            Clicked="MissionPlayPlauseClick"
                            WidthRequest="75"
                            Text="Pause" />
                    <Button x:Name="CameraControlButton"
                            Clicked="ToggleFollowPlane"
                            WidthRequest="110"
                            Text="Don't Follow" />
                </StackLayout>
                <Label Text="Mission Progress:" />
                <Slider x:Name="MissionProgressBar"
                        Maximum="100"
                        HorizontalOptions="Start"
                        WidthRequest="350"
                        ValueChanged="MissionProgressOnSeek" />
                <Grid ColumnDefinitions="100,*">
                    <StackLayout Orientation="Vertical">
                        <Label>Altitude:</Label>
                        <Label x:Name="AltitudeLabel" />
                        <Label>Heading:</Label>
                        <Label x:Name="HeadingLabel" />
                        <Label>Pitch:</Label>
                        <Label x:Name="PitchLabel" />
                        <Label>Roll:</Label>
                        <Label x:Name="RollLabel" />
                    </StackLayout>
                    <esriUI:MapView x:Name="InsetMapView"
                                    Grid.Column="1"
                                    HeightRequest="150"
                                    HorizontalOptions="End"
                                    IsAttributionTextVisible="False"
                                    VerticalOptions="Start"
                                    WidthRequest="150" />
                </Grid>
            </StackLayout>
        </Border>
    </Grid>
</ContentPage>