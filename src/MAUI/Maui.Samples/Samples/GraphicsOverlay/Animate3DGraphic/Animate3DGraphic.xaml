<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="ArcGISRuntime.Samples.Animate3DGraphic.Animate3DGraphic"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:esriUI="clr-namespace:Esri.ArcGISRuntime.Maui;assembly=Esri.ArcGISRuntime.Maui">
    <Grid>
        <esriUI:SceneView x:Name="MySceneView" AtmosphereEffect="Realistic" />
        <Frame Margin="5"
               HorizontalOptions="Start"
               VerticalOptions="Start">
            <StackLayout Orientation="Vertical" Spacing="5">
                <Picker x:Name="MissionSelectionBox" WidthRequest="125" />
                <Button x:Name="MissionPlayPause"
                        Clicked="MissionPlayPlauseClick"
                        Text="Pause" />
                <Button Clicked="ToggleFollowPlane" Text="Don't Follow" />
            </StackLayout>
        </Frame>

        <Frame x:Name="LayoutFrame"
               Margin="5"
               HorizontalOptions="End"
               VerticalOptions="Start">
            <StackLayout Padding="5">
                <Label Text="Mission Progress:" />
                <Slider x:Name="MissionProgressBar"
                        Maximum="100"
                        ValueChanged="MissionProgressOnSeek" />
                <Label>Altitude:</Label>
                <Label x:Name="AltitudeLabel" />
                <Label>Heading:</Label>
                <Label x:Name="HeadingLabel" />
                <Label>Pitch:</Label>
                <Label x:Name="PitchLabel" />
                <Label>Roll:</Label>
                <Label x:Name="RollLabel" />
            </StackLayout>
        </Frame>
        <esriUI:MapView x:Name="InsetMapView"
                        Margin="20"
                        HeightRequest="150"
                        HorizontalOptions="Start"
                        IsAttributionTextVisible="False"
                        VerticalOptions="End"
                        WidthRequest="150" />
    </Grid>
</ContentPage>