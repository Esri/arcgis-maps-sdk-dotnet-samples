<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="ArcGISRuntime.Samples.ChangeBlendRenderer.ChangeBlendRenderer"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:esriUI="clr-namespace:Esri.ArcGISRuntime.Maui;assembly=Esri.ArcGISRuntime.Maui">
    <Grid Style="{DynamicResource EsriSampleContainer}">
        <esriUI:MapView x:Name="MyMapView" Style="{DynamicResource EsriSampleGeoView}" />
        <Border Style="{DynamicResource EsriSampleControlPanel}">
            <Grid Padding="5" RowDefinitions="auto,auto,auto,auto,auto">
                <StackLayout Grid.Row="0"
                             Orientation="Horizontal"
                             Spacing="5">
                    <Label x:Name="Label_Altitude"
                           Text="Altitude"
                           VerticalTextAlignment="Center" />
                    <Slider x:Name="Altitude_Slider" WidthRequest="100" />
                </StackLayout>
                <StackLayout Grid.Row="1"
                             Orientation="Horizontal"
                             Spacing="5">
                    <Label x:Name="Label_Azimuth"
                           Text="Azimuth"
                           VerticalTextAlignment="Center" />
                    <Slider x:Name="Azimuth_Slider" WidthRequest="100" />
                </StackLayout>
                <StackLayout Grid.Row="2" Orientation="Horizontal">
                    <Label x:Name="Label_SlopeTypes" Text="Slope Type" />
                    <ListView x:Name="SlopeTypes" HeightRequest="90" />
                </StackLayout>
                <StackLayout Grid.Row="3" Orientation="Horizontal">
                    <Label x:Name="Label_ColorRamps" Text="Color Ramp" />
                    <ListView x:Name="ColorRamps" HeightRequest="90" />
                </StackLayout>
                <Button x:Name="UpdateRenderer"
                        Grid.Row="4"
                        Clicked="OnUpdateRendererClicked"
                        IsEnabled="True"
                        Text="Update Renderer" />
            </Grid>
        </Border>
    </Grid>
</ContentPage>