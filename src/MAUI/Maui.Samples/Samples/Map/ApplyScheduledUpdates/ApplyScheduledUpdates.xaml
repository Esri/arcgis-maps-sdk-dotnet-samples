<ContentPage x:Class="ArcGISRuntime.Samples.ApplyScheduledUpdates.ApplyScheduledUpdates"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cmp="clr-namespace:Microsoft.Maui.Controls.Compatibility;assembly=Microsoft.Maui.Controls"
             xmlns:esriUI="clr-namespace:Esri.ArcGISRuntime.Maui;assembly=Esri.ArcGISRuntime.Maui"
             xmlns:resources="clr-namespace:ArcGISRuntimeMaui.Resources">
    <Grid>
        <esriUI:MapView x:Name="MyMapView" />
        <Border Margin="5"
                BackgroundColor="{AppThemeBinding Light=#dfdfdf, Dark=#303030}"
                StrokeShape="RoundRectangle 5"
                HorizontalOptions="End"
                VerticalOptions="Start"
                WidthRequest="300">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <Button x:Name="ApplyButton"
                        Grid.Row="0"
                        Clicked="ApplyButton_Clicked"
                        IsEnabled="False"
                        Text="Apply scheduled updates" />
                <Label x:Name="InfoLabel"
                       Grid.Row="1"
                       Margin="5" />
            </Grid>
        </Border >
    </Grid>
</ContentPage>