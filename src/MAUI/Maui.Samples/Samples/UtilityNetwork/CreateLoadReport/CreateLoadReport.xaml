<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="ArcGIS.Samples.CreateLoadReport.CreateLoadReport"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:esriUI="clr-namespace:Esri.ArcGISRuntime.Maui;assembly=Esri.ArcGISRuntime.Maui"
             xmlns:toolkit="using:Esri.ArcGISRuntime.Toolkit.Maui">
    <Border Style="{DynamicResource EsriSampleControlPanel}">
        <VerticalStackLayout Spacing="5">
            <Label Text="Select a phase using the dropdown, add, then run report." />
            <Picker x:Name="PhasesList"
                    ItemDisplayBinding="{Binding Name}" />
            <Button Clicked="OnAddPhase" Text="Add" />
            <Label x:Name="Phases"
                   BackgroundColor="{AppThemeBinding Dark=Gray}"
                   HorizontalTextAlignment="Center" />
            <Button Clicked="OnRunReport" Text="Run Report" />
            <Button Clicked="OnReset" Text="Reset" />
            <CollectionView x:Name="ReportView"
                            Margin="0,10,0,0"
                            BackgroundColor="{AppThemeBinding Dark=Gray}">
                <CollectionView.Header>
                    <Grid ColumnSpacing="10" HorizontalOptions="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Text="Phase" />
                        <Label Grid.Column="1" Text="Total Customers" />
                        <Label Grid.Column="2" Text="Total Load" />
                    </Grid>
                </CollectionView.Header>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout HorizontalOptions="Center" Spacing="50">
                            <Label FontSize="Medium" Text="{Binding Phase}" />
                            <Label FontSize="Medium" Text="{Binding TotalCustomers}" />
                            <Label FontSize="Medium" Text="{Binding TotalLoad}" />
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </Border>
</ContentPage>