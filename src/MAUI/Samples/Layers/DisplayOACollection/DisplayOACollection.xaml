<ContentPage x:Class="ArcGISRuntime.Samples.DisplayOACollection.DisplayOACollection"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cmp="clr-namespace:Microsoft.Maui.Controls.Compatibility;assembly=Microsoft.Maui.Controls"
             xmlns:esriUI="clr-namespace:Esri.ArcGISRuntime.Maui;assembly=Esri.ArcGISRuntime.Maui"
             xmlns:resources="clr-namespace:ArcGISRuntimeMaui.Resources">
    <cmp:RelativeLayout>
        <esriUI:MapView x:Name="MyMapView"
                        BindingContext="{x:Reference Name=ResponsiveFormContainer}"
                        Style="{StaticResource MapWithFormStyle}" />
        <resources:ResponsiveFormContainer x:Name="ResponsiveFormContainer">
            <StackLayout>
                <Label Grid.Row="1"
                       Margin="5"
                       Text="Pan and zoom to see features." />
                <ActivityIndicator x:Name="LoadingProgressBar"
                                   Margin="5"
                                   IsRunning="True" />
            </StackLayout>
        </resources:ResponsiveFormContainer>
    </cmp:RelativeLayout>
</ContentPage>