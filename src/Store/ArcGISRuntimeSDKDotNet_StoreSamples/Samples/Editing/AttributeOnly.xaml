<Page x:Class="ArcGISRuntimeSDKDotNet_StoreSamples.Samples.AttributeOnly"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ArcGISRuntimeSDKDotNet_StoreSamples.Common"
      xmlns:esri="using:Esri.ArcGISRuntime.Controls"
      xmlns:layers="using:Esri.ArcGISRuntime.Layers"
      xmlns:symbology="using:Esri.ArcGISRuntime.Symbology">
    <UserControl.Resources>
        <symbology:SimpleLineSymbol x:Key="HighlightOutlineSymbol"
                                    Color="Yellow"
                                    Width="4.5" />
        <symbology:SimpleFillSymbol x:Key="HighlightFillSymbol"
                                    Color="Transparent"
                                    Outline="{StaticResource HighlightOutlineSymbol}" />
        <symbology:SimpleRenderer x:Key="HighlightRenderer"
                                  Symbol="{StaticResource HighlightFillSymbol}" />
        <local:KeyValueConverter x:Key="KeyValueConverter" />
    </UserControl.Resources>
    <Grid>
        <esri:MapView x:Name="MyMapView"
                      LayerLoaded="MyMapView_LayerLoaded"
                      MapViewTapped="MyMapView_MapViewTapped">
            <esri:Map>
                <esri:Map.InitialViewpoint>
                    <esri:ViewpointExtent XMin="-13075150.8872716"
                                          YMin="4015586.5093488"
                                          XMax="-13073624.4294544"
                                          YMax="4016213.20181174" />
                </esri:Map.InitialViewpoint>
                <layers:ArcGISTiledMapServiceLayer ServiceUri="http://services.arcgisonline.com/ArcGIS/rest/services/World_Street_Map/MapServer" />
                <layers:ArcGISDynamicMapServiceLayer ID="PoolPermitDynamicLayer"
                                                     ServiceUri="http://sampleserver6.arcgisonline.com/arcgis/rest/services/PoolPermits/MapServer" />
                <layers:GraphicsLayer ID="PoolPermitFeatureLayer"
                                      Renderer="{StaticResource HighlightRenderer}" />
            </esri:Map>
        </esri:MapView>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar IsOpen="True"
                    IsSticky="True">
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="Important"
                              Label="Info">
                    <AppBarButton.Flyout>
                        <Flyout>
                            <TextBlock Text="Tap on a feature in the map to edit its attribute. Confirm edit to save to the database."
                                       Width="200"
                                       TextAlignment="Left"
                                       Margin="30,20,20,30"
                                       TextWrapping="Wrap" />
                        </Flyout>
                    </AppBarButton.Flyout>
                </AppBarButton>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
