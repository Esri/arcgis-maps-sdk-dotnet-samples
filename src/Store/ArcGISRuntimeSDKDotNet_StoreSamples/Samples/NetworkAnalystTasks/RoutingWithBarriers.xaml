<Page
    x:Class="ArcGISRuntimeSDKDotNet_StoreSamples.Samples.RoutingWithBarriers"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:ArcGISRuntimeSDKDotNet_StoreSamples.Common"
    xmlns:esri="using:Esri.ArcGISRuntime.Controls"
    xmlns:lyr="using:Esri.ArcGISRuntime.Layers"
    xmlns:sym="using:Esri.ArcGISRuntime.Symbology">
    <Grid x:Name="LayoutRoot" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.Resources>
            <sym:SimpleLineSymbol x:Key="RouteSymbol" Color="#990000FF" Style="Solid" Width="3" />
            <sym:SimpleRenderer x:Key="RouteLayerRenderer" Symbol="{StaticResource RouteSymbol}" />

            <sym:SimpleMarkerSymbol x:Key="BarrierSymbol" Color="Black" Style="X" Size="20" />
            <sym:SimpleRenderer x:Key="BarrierLayerRenderer" Symbol="{StaticResource BarrierSymbol}" />
        </Grid.Resources>

        <esri:MapView x:Name="mapView" MapViewTapped="mapView_MapViewTapped">
            <esri:Map>
                <lyr:ArcGISTiledMapServiceLayer
                    ServiceUri="http://services.arcgisonline.com/ArcGIS/rest/services/World_Street_Map/MapServer" />

                <lyr:GraphicsLayer ID="RouteGraphicsLayer" Renderer="{StaticResource RouteLayerRenderer}" />
                <lyr:GraphicsLayer ID="StopGraphicsLayer" />
                <lyr:GraphicsLayer ID="BarrierGraphicsLayer" Renderer="{StaticResource BarrierLayerRenderer}" />
            </esri:Map>
        </esri:MapView>

        <Border Background="#CC000000" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="30" Padding="20" Width="300">
            <StackPanel>
                <TextBlock Text="Tap stop and barrier points on the map to see the calculated route." 
                           TextWrapping="Wrap" FontSize="13" />

                <StackPanel Orientation="Horizontal" Margin="0,12,0,0" HorizontalAlignment="Center">
                    <RadioButton x:Name="rbStops" Content="Add Stops" Margin="0,0,20,0" IsChecked="True" />
                    <RadioButton x:Name="rbBarriers" Content="Add Barriers" />
                </StackPanel>

                <Button Content="Clear" Click="ClearButton_Click" HorizontalAlignment="Center" Padding="20,4" Margin="0,12,0,0" />

                <ProgressBar x:Name="progress" IsIndeterminate="True" Visibility="Collapsed" Margin="0,12,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
