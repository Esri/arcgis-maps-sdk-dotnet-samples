<Page
    x:Class="ArcGISRuntime.UWP.Viewer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:models="using:ArcGISRuntime.Samples.Shared.Models"
    xmlns:viewer="using:ArcGISRuntime.UWP.Viewer"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="SmallFeaturedListItemTemplate">
            <Grid>
                <TextBlock Text="{Binding Name}" FontSize="18" Grid.Column="1" Margin="10,5,10,5" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="StandardSampleItemTemplate">
            <Border Background="{StaticResource PrimaryBackgroundColor}" BorderBrush="{StaticResource AppBarBorderThemeBrush}" BorderThickness="1" Margin="1">
                <Grid Tapped="OnSampleItemTapped" Width="285" Margin="2" Height="285">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="200" />
                        <RowDefinition Height="35" />
                        <RowDefinition Height="60" />
                    </Grid.RowDefinitions>
                    <Image Width="280" Height="200" Stretch="Uniform"
                       HorizontalAlignment="Center"
                       Source="{Binding SampleImageName, Converter={StaticResource sampleToBitmapConverter}}">
                    </Image>
                    <TextBlock Text="{Binding SampleName}" FontSize="18" Grid.Row="1" Margin="10,5,10,5"  Style="{StaticResource HeaderTextBlock}" />
                    <TextBlock Text="{Binding Description}" FontSize="13"
										   TextWrapping="Wrap" Grid.Row="2" VerticalAlignment="Top"
										   Margin="10,-5,10,5" FontWeight="Normal" TextTrimming="CharacterEllipsis"  Style="{StaticResource PrimaryTextBlock}" />
                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="SmallSampleItemTemplate">
            <Border
                    Background="{StaticResource PrimaryBackgroundColor}"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    BorderBrush="{StaticResource AppBarBorderThemeBrush}" BorderThickness="1" Margin="0,2,0,2">
                <Grid Tapped="OnSampleItemTapped"
                    HorizontalAlignment="Stretch" Height="48">
                    <TextBlock Text="{Binding SampleName}" HorizontalAlignment="Stretch" VerticalAlignment="Center" FontSize="18" Margin="10,0"
                               Style="{StaticResource PrimaryTextBlock}" />
                    <Button
                            Tapped="OnInfoTapped"
                            Click="OnInfoClicked"
                            Style="{StaticResource GeneralButtonStyle}"
                            HorizontalAlignment="Right">
                        <Grid>
                            <Ellipse Height="30" Width="30" Style="{StaticResource EllipseStyle}" StrokeThickness="1"
                                     HorizontalAlignment="Center" VerticalAlignment="Center" />
                            <TextBlock Text="i" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Style="{StaticResource HeaderTextBlock}" />
                        </Grid>
                    </Button>
                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="CategoryTemplate" x:DataType="models:SearchableTreeNode">
            <TextBlock Text="{Binding Content.Name}" />
        </DataTemplate>

        <DataTemplate x:Key="SampleTemplate" x:DataType="models:SearchableTreeNode">
            <TextBlock Text="{Binding Content.SampleName}" />
        </DataTemplate>

        <viewer:TreeViewItemTemplateSelector
            x:Key="TreeViewItemTemplateSelector"
            CategoryTemplate="{StaticResource CategoryTemplate}"
            SampleTemplate="{StaticResource SampleTemplate}" />
    </Page.Resources>

    <Grid Background="Transparent">
        <!--
        ============================
        Adaptive triggers
        ============================
        -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <!--
                ============================
                Handle screen less than some number of pixels
                ============================
                -->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="SamplesListView.Visibility" Value="Visible" />
                        <Setter Target="SamplesGridView.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
                <!--
                ============================
                Handle screen over some number of mixels
                ============================
                -->
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="920" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="HeaderArea"
              Background="{StaticResource HeaderBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="ArcGIS Runtime SDK for .NET (UWP)" Foreground="White" FontSize="18"
                       FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0" Grid.Column="0"
                       />
            <AutoSuggestBox Grid.Column="1"
                            x:Name="SearchBox" RelativePanel.AlignRightWithPanel="True"
                            RelativePanel.AlignVerticalCenterWith="CategoryTitle"
                            Width="240" PlaceholderText="i.e. ArcGISTileLayer" Margin="20,0"
                            TextChanged="OnSearchQuerySubmitted"

                            QueryIcon="Find"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center" />

            <Button Grid.Column="2" Margin="5"
                    HorizontalAlignment="Right" HorizontalContentAlignment="Center"
                    VerticalContentAlignment="Center" VerticalAlignment="Center"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="Settings_Click">
                <Image Source="Assets/ic_menu_settings_dark.png" Height="32" Width="32" />
            </Button>
        </Grid>

        <!--
        ============================
        Main content region
        ============================
        -->

        <Grid   x:Name="MainContentRegion" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <muxc:TreeView     Grid.Column="0"
                               x:Name="CategoriesTree"
                               SelectionMode ="Single"
                               ItemInvoked="CategoriesTree_ItemInvoked"
                               ItemTemplateSelector="{StaticResource TreeViewItemTemplateSelector}"
                               CanDrag="False"
                               CanDragItems="False"
                               CanReorderItems="False" />
            <ContentControl Grid.Column="1" Margin="5" x:Name="SamplePageContainer" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />
            <Grid     Grid.Column="1"
                      x:Name="SampleSelectionGrid"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch">

                <GridView x:Name="SamplesGridView"
                              ItemTemplate="{StaticResource StandardSampleItemTemplate}"
                              SelectionMode="None"
                              Background="Transparent" Margin="1,0,0,0">
                    <GridView.ItemContainerStyle>
                        <Style TargetType="GridViewItem">
                            <Setter Property="HorizontalAlignment" Value="Center" />
                        </Style>
                    </GridView.ItemContainerStyle>
                </GridView>

                <ListView x:Name="SamplesListView" Visibility="Collapsed"
                              SelectionMode="None"
                              ItemTemplate="{StaticResource SmallSampleItemTemplate}">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</Page>