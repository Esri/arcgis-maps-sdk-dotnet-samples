<UserControl
    x:Class="ArcGISRuntime.UWP.Samples.Buffer.Buffer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:esriUI="using:Esri.ArcGISRuntime.UI.Controls">
    <Grid>
        <esriUI:MapView x:Name="MyMapView" />
        <Border BorderBrush="Black" BorderThickness="1" Background="White"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="30" Padding="20" Width="375">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Text="Specify the buffer in miles, then tap to place the buffered points."
                           TextWrapping="Wrap" FontWeight="SemiBold"
                           Grid.Row="0" />
                <TextBox x:Name="BufferDistanceMilesTextBox"
                         Grid.Row="1"
                         HorizontalAlignment="Stretch" HorizontalTextAlignment="Center"
                         Margin="0,5,0,0"
                         Text="10" />
            </Grid>
        </Border>
    </Grid>
</UserControl>