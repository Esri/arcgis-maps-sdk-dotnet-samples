<UserControl
    x:Class="ArcGISRuntime.UWP.Samples.ChangeSublayerRenderer.ChangeSublayerRenderer"
    x:Name="_this"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:esriUI="using:Esri.ArcGISRuntime.UI.Controls">
    <Grid>
        <esriUI:MapView x:Name="MyMapView" />
        <Border
            Background="White"
            BorderBrush="Black" BorderThickness="1"
            HorizontalAlignment="Right" VerticalAlignment="Top"
            Margin="10"
            Padding="5"
            Width="375">
            <StackPanel>
                <TextBlock x:Name="ChangeSublayerRendererInstructionsLabel" 
                           Text="Click the 'Change Sublayer Renderer' button to apply a unique value renderer to the counties sub-layer."
                           TextWrapping="Wrap"
                           Margin="0,0,0,10"/>
                <Button x:Name="ChangeSublayerRendererButton" 
                        Content="Change Sublayer Renderer"
                        HorizontalAlignment="Stretch"
                    Click="ChangeSublayerRendererButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>