<UserControl
    x:Class="ArcGISRuntime.UWP.Samples.OfflineGeocode.OfflineGeocode"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:esriUI="using:Esri.ArcGISRuntime.UI.Controls">
    <Grid>
        <esriUI:MapView x:Name="MyMapView" />
        <Border Style="{StaticResource BorderStyle}">
            <StackPanel>
                <TextBlock
                    FontWeight="SemiBold"
                    IsColorFontEnabled="False"
                    Text="Enter an address and tap 🔎 to search. Start typing for suggestions."
                    TextAlignment="Center"
                    TextWrapping="Wrap" />
                <AutoSuggestBox
                    x:Name="AutoSuggestBox"
                    Margin="0,5,0,0"
                    HorizontalAlignment="Stretch"
                    IsEnabled="False"
                    ItemsSource="{x:Bind _addresses}"
                    QueryIcon="Find"
                    QuerySubmitted="AutoSuggestBox_OnQuerySubmitted" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

