<UserControl x:Class="ArcGISRuntime.WPF.Samples.ChangeFeatureLayerRenderer.ChangeFeatureLayerRenderer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ToolBarTray Grid.Row="0">
            <ToolBar>
                <Button Name="Reset" Content="Reset" Click="OnResetButtonClicked"/>
                <Button Name="ApplyRenderer" Content="Override" Click="OnOverrideButtonClicked"/>
            </ToolBar>
        </ToolBarTray>
        <esri:MapView x:Name="MyMapView" Grid.Row="1"/>
    </Grid>
</UserControl>
