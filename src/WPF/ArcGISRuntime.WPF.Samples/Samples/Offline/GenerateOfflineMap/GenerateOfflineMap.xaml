<UserControl 
    x:Class="ArcGISRuntime.WPF.Samples.GenerateOfflineMap.GenerateOfflineMap"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013" 
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid>
        <esri:MapView x:Name="MyMapView"/>

        <Rectangle x:Name="areaOfInterestFrame" Fill="Transparent" 
                   Stroke="Red" StrokeThickness="2"
                   IsHitTestVisible="False"
                   Margin="100"/>

        <Border x:Name="takeOfflineArea"
                Background="White" BorderBrush="Black" BorderThickness="1"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="30" Padding="20" Width="375">
            <StackPanel>
                <Button x:Name="TakeMapOfflineButton" Click="OnTakeMapOfflineClicked" IsEnabled="True">Take map offline</Button>
            </StackPanel>
        </Border>

        <Border x:Name="offlineArea" Visibility="Collapsed"
                Background="White" BorderBrush="Black" BorderThickness="1"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="30" Padding="5" Width="450">
            <StackPanel>
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Text="Map offline!" Margin="10"/>
            </StackPanel>
        </Border>

        <Grid x:Name="busyIndicator" Background="#807f7f7f" Visibility="Collapsed">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Foreground="White" Margin="10" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Run Text="Generating offline map... "></Run>
                    <Run x:Name="Percentage" Text=""></Run>
                </TextBlock>
                <ProgressBar x:Name="progressBar" Minimum="0" Maximum="100"
                             IsEnabled="True" IsIndeterminate="True" Width="100" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" Height="10" Margin="0,0,0,10"/>
                <Button Content="Cancel" Click="OnCancelClicked" Width="100"  Grid.Row="3" HorizontalAlignment="Center"/>
            </Grid>
        </Grid>
        <Grid x:Name="loadingIndicator" Background="#807f7f7f" Visibility="Visible">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Loading online map..." Foreground="White" Margin="10" FontSize="18"/>
                <ProgressBar IsEnabled="True" IsIndeterminate="True" Width="100" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>

