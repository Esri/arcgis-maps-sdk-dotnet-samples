<UserControl x:Class="ArcGISRuntime.WPF.Samples.ToggleBetweenFeatureRequestModes.ToggleBetweenFeatureRequestModes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013">
    <Grid>
        <esri:MapView x:Name="MyMapView" />
        <Border Width="175"
                Height="auto"
                Style="{StaticResource BorderStyle}">
            <StackPanel>
                <RadioButton x:Name="Cache"
                             Content="Cache"
                             IsChecked="True" />
                <RadioButton x:Name="NoCache" Content="No Cache" />
                <RadioButton x:Name="ManualCache" Content="Manual Cache" />
                <Button x:Name="PopulateMap"
                        Click="PopulateButtonClick"
                        Content="Populate" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>