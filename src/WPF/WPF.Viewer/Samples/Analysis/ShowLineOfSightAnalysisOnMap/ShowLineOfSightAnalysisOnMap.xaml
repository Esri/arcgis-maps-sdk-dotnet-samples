<UserControl x:Class="ArcGIS.WPF.Samples.ShowLineOfSightAnalysisOnMap.ShowLineOfSightAnalysisOnMap"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013">
    <Grid>
        <esri:MapView x:Name="MyMapView" />
        <Border Style="{StaticResource BorderStyle}"
                MaxWidth="620">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="250" />
                </Grid.RowDefinitions>
                <CheckBox x:Name="VisibleOnlyCheckBox"
                          Grid.Row="0"
                          Content="Show line of sight where target (circle) is visible from observer (triangle)"
                          Checked="VisibleOnlyCheckBox_Changed"
                          Unchecked="VisibleOnlyCheckBox_Changed" />
                <Separator Grid.Row="1" Margin="0,4" />
                <ScrollViewer Grid.Row="2"
                              VerticalScrollBarVisibility="Visible">
                    <ItemsControl x:Name="ResultsItemsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Rectangle Grid.Column="0"
                                               Width="16" Height="16"
                                               Fill="{Binding ObserverBrush}"
                                               HorizontalAlignment="Left"
                                               Margin="4,0,0,0" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding InfoText}"
                                               TextWrapping="Wrap"
                                               Margin="4,2" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
        <TextBlock Margin="0,0,10,30"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Bottom"
                   FontSize="11"
                   FontStyle="Italic"
                   Foreground="White"
                   Text="Raster data Copyright Scottish Government and SEPA (2014)"
                   TextWrapping="Wrap" />
    </Grid>
</UserControl>
