<UserControl x:Class="ArcGIS.WPF.Samples.ListRelatedFeatures.ListRelatedFeatures"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013">
    <Grid>
        <esri:MapView x:Name="MyMapView" />
        <Border Style="{StaticResource BorderStyle}">
            <StackPanel>
                <TextBlock Margin="0,0,0,10"
                           FontWeight="SemiBold"
                           Text="Tap a park (green) to see related features."
                           TextAlignment="Center" />
                <ProgressBar x:Name="LoadingProgress"
                             Height="15"
                             Margin="0,0,0,10"
                             IsIndeterminate="True"
                             Visibility="Visible" />
                <ListBox x:Name="MyResultsView"
                          MaxHeight="300"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource CalciteListBoxItemStyle}">
                            <Setter Property="Focusable" Value="false" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>