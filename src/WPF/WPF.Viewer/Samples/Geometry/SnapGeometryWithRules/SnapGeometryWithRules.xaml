<UserControl x:Class="ArcGIS.WPF.Samples.SnapGeometryWithRules.SnapGeometryWithRules"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:ArcGIS.WPF.Viewer.Converters"
             xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013">
    <UserControl.Resources>
        <DataTemplate x:Key="SnapSettingTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Width="150"
                           Text="{Binding Path=Name}" />
                <CheckBox Grid.Column="1" IsChecked="{Binding Path=IsEnabled}" />
            </Grid>
        </DataTemplate>
        <Style x:Key="IconStyle" TargetType="Button">
            <Style.Setters>
                <Setter Property="FontFamily" Value="pack://application:,,,/Resources/Fonts/#calcite-ui-icons-24" />
                <Setter Property="FontSize" Value="25" />
                <Setter Property="Background" Value="White" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="Padding" Value="3" />
                <Setter Property="Margin" Value="3" />
            </Style.Setters>
        </Style>
        <converters:BoolNegationConverter x:Key="BoolNegationConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <esri:MapView x:Name="MyMapView" GeoViewTapped="MyMapView_GeoViewTapped" />
        <Border x:Name="SnappingControls"
                Width="300"
                Margin="10"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="White"
                Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <ScrollViewer Grid.Row="0" Height="400">
                    <StackPanel Margin="10" Orientation="Vertical">
                        <TextBlock Padding="0,5"
                                   FontSize="14"
                                   Text="Tap a feature to edit or pick an asset type to create"
                                   TextWrapping="Wrap" />
                        <TextBlock x:Name="SelectedFeatureLabel"
                                   Padding="0,5"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Text="Feature selected:"
                                   Visibility="Collapsed" />
                        <Grid x:Name="SelectedAssetTypeGrid" Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Margin="0,0,5,0"
                                       Text="AssetType:" />
                            <TextBlock x:Name="SelectedAssetTypeLabel"
                                       Grid.Row="0"
                                       Grid.Column="1" />
                        </Grid>
                        <TextBlock x:Name="AssetTypePickerLabel"
                                   Padding="0,5"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Text="Choose an asset type" />
                        <ComboBox x:Name="AssetTypeComboBox"
                                  Width="200"
                                  SelectionChanged="AssetTypeComboBox_SelectionChanged">
                            <ComboBoxItem Content="Automatic Reset" />
                            <ComboBoxItem Content="Plastic 3-Way" />
                        </ComboBox>
                        <Button x:Name="GeometryEditorButton"
                                Width="200"
                                Click="GeometryEditorButton_Click"
                                Content="Start editor"
                                IsEnabled="{Binding GeometryEditor.IsStarted, ElementName=MyMapView, Converter={StaticResource BoolNegationConverter}}" />
                        <Grid Width="205">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.5*" />
                                <ColumnDefinition Width="0.5*" />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Click="DiscardButton_Click"
                                    Content="&#xe080;"
                                    IsEnabled="{Binding GeometryEditor.IsStarted, ElementName=MyMapView}"
                                    Style="{StaticResource IconStyle}"
                                    ToolTipService.ToolTip="Discard edits" />
                            <Button x:Name="SaveButton"
                                    Grid.Column="1"
                                    Click="SaveButton_Click"
                                    Content="&#xe06f;"
                                    IsEnabled="{Binding GeometryEditor.CanUndo, ElementName=MyMapView}"
                                    Style="{StaticResource IconStyle}"
                                    ToolTipService.ToolTip="Save edits" />
                        </Grid>
                        <TextBlock Padding="0,5"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Text="Snap sources" />
                        <ListView x:Name="SnapSourcesList"
                                  Width="200"
                                  Height="80"
                                  ItemTemplate="{StaticResource SnapSettingTemplate}" />
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
