<UserControl x:Class="ArcGIS.WPF.Samples.FilterByTimeExtent.FilterByTimeExtent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <esri:MapView x:Name="MyMapView" Grid.Row="0" />
        <esri:TimeSlider x:Name="MyTimeSlider"
                         Grid.Row="1"
                         MaxWidth="800"
                         Margin="0,20"
                         CurrentExtentChanged="TimeSlider_CurrentExtentChanged" />
    </Grid>
</UserControl>