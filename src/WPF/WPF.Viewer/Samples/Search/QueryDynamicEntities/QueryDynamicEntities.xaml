<UserControl x:Class="ArcGIS.WPF.Samples.QueryDynamicEntities.QueryDynamicEntities"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013">
    <Grid>
        <esri:MapView x:Name="MyMapView" />

        <!--  Query Controls  -->
        <Border Width="300" Style="{StaticResource BorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Margin="0,0,8,0"
                           VerticalAlignment="Center"
                           FontWeight="Bold"
                           Text="Query flights" />

                <ComboBox x:Name="QueryDropdown"
                          Grid.Column="1"
                          MaxWidth="200"
                          SelectionChanged="OnQuerySelectionChanged">
                    <ComboBoxItem Content="Within 15 Miles of PHX" Tag="QueryGeometry" />
                    <ComboBoxItem Content="Arriving in PHX" Tag="QueryAttributes" />
                    <ComboBoxItem Content="With Flight Number" Tag="QueryTrackId" />
                </ComboBox>
            </Grid>
        </Border>

        <!--  Query Results Panel  -->
        <Border x:Name="ResultsPanel"
                Width="300"
                Style="{StaticResource BorderStyle}"
                Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Margin="0,0,0,8"
                           FontSize="14"
                           FontWeight="Bold"
                           Text="Query Results" />

                <TextBlock x:Name="ResultsDescription"
                           Grid.Row="1"
                           Margin="0,0,0,8"
                           FontStyle="Italic"
                           TextWrapping="Wrap" />

                <ScrollViewer Grid.Row="2"
                              MaxHeight="280"
                              VerticalScrollBarVisibility="Auto">
                    <ListBox x:Name="ResultsList"
                             SelectionChanged="OnResultSelectionChanged"
                             SelectionMode="Single">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Expander Header="{Binding FlightNumber, Mode=OneWay}">
                                    <StackPanel Margin="20,5,0,5">
                                        <TextBlock>
                                            <Run FontWeight="Bold" Text="Aircraft: " />
                                            <Run Text="{Binding Aircraft, Mode=OneWay}" />
                                        </TextBlock>
                                        <TextBlock>
                                            <Run FontWeight="Bold" Text="Altitude: " />
                                            <Run Text="{Binding Altitude, Mode=OneWay}" />
                                            <Run Text=" ft" />
                                        </TextBlock>
                                        <TextBlock>
                                            <Run FontWeight="Bold" Text="Speed: " />
                                            <Run Text="{Binding Speed, Mode=OneWay}" />
                                            <Run Text=" mph" />
                                        </TextBlock>
                                        <TextBlock>
                                            <Run FontWeight="Bold" Text="Heading: " />
                                            <Run Text="{Binding Heading, Mode=OneWay}" />
                                            <Run Text="°" />
                                        </TextBlock>
                                        <TextBlock>
                                            <Run FontWeight="Bold" Text="Status: " />
                                            <Run Text="{Binding Status, Mode=OneWay}" />
                                        </TextBlock>
                                        <TextBlock>
                                            <Run FontWeight="Bold" Text="Arrival: " />
                                            <Run Text="{Binding ArrivalAirport, Mode=OneWay}" />
                                        </TextBlock>
                                    </StackPanel>
                                </Expander>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>

                <Button Grid.Row="3"
                        Margin="0,8,0,0"
                        Padding="12,4"
                        HorizontalAlignment="Right"
                        Click="OnCloseResults"
                        Content="Close" />
            </Grid>
        </Border>

        <!--  Flight Number Input Dialog  -->
        <Border x:Name="FlightNumberDialog"
                Width="300"
                Style="{StaticResource BorderStyle}"
                Visibility="Collapsed">
            <StackPanel>
                <TextBlock Margin="0,0,0,12"
                           FontSize="14"
                           FontWeight="Bold"
                           Text="Enter Flight Number" />
                <TextBox x:Name="FlightNumberInput" Tag="Flight_1107" />
                <TextBlock Margin="0,8,0,0"
                           FontSize="11"
                           FontStyle="Italic"
                           Foreground="Gray"
                           Text="Example: Flight_1107" />
                <StackPanel Margin="0,12,0,0"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">
                    <Button Margin="0,0,8,0"
                            Padding="12,4"
                            Click="OnFlightNumberQuery"
                            Content="Query" />
                    <Button Padding="12,4"
                            Click="OnFlightNumberCancel"
                            Content="Cancel" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>