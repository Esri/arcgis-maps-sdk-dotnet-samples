<UserControl
    x:Class="ArcGIS.WinUI.Viewer.Samples.EditFeaturesUsingFeatureForms.EditFeaturesUsingFeatureForms"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:esri="using:Esri.ArcGISRuntime.UI.Controls"
    xmlns:toolkit="using:Esri.ArcGISRuntime.Toolkit.UI.Controls">

    <Grid>
        <esri:MapView x:Name="MyMapView" GeoViewTapped="MyMapView_GeoViewTapped" />
        
        <!-- Define the ContentDialog in XAML -->
        <ContentDialog x:Name="EditFeatureDialog"
                       Title="Edit feature"
                       PrimaryButtonText="Save"
                       CloseButtonText="Cancel"
                       Visibility="Collapsed"
                       IsPrimaryButtonEnabled="{Binding ElementName=FeatureFormViewPanel, Path=IsValid}">
            <ScrollViewer>
                <toolkit:FeatureFormView x:Name="FeatureFormViewPanel" />
            </ScrollViewer>
        </ContentDialog>
    </Grid>
</UserControl>
