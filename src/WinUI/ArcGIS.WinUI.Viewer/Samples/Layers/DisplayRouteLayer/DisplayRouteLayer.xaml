<UserControl x:Class="ArcGIS.WinUI.Samples.DisplayRouteLayer.DisplayRouteLayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="using:ArcGIS.WinUI.Viewer.Converters"
             xmlns:esri="using:Esri.ArcGISRuntime"
             xmlns:esriUI="using:Esri.ArcGISRuntime.UI.Controls">

    <UserControl.Resources>
        <converters:DirectionPointTypeToIconConverter x:Key="Converter" />
    </UserControl.Resources>
    <Grid>
        <esriUI:MapView x:Name="MyMapView" />
        <Border x:Name="Border"
                Width="650"
                Style="{StaticResource BorderStyle}"
                Visibility="Collapsed">
            <ItemsControl x:Name="DirectionsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Attributes[Sequence]}" />
                            <TextBlock Text="." />
                            <TextBlock Margin="0,0,5,5"
                                       FontFamily="pack://application:,,,/Fonts/#calcite-ui-icons-24"
                                       Text="{Binding Attributes[DirectionPointType]}" />
                            <TextBlock Text="{Binding Attributes[DisplayText]}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </Grid>

</UserControl>