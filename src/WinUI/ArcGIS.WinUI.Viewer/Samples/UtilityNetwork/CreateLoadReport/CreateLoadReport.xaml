<UserControl x:Class="ArcGIS.WinUI.Samples.CreateLoadReport.CreateLoadReport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
             xmlns:esri="using:Esri.ArcGISRuntime"
             xmlns:esriUI="using:Esri.ArcGISRuntime.UI.Controls">

    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="Phases"
                   VerticalAlignment="Center"
                   FontSize="20"
                   TextAlignment="Center" />
        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                       HorizontalAlignment="Stretch"
                       FontWeight="Bold"
                       Text="Select a phase type, then click 'Add'"
                       TextAlignment="Center" />
            <ComboBox x:Name="PhasesList"
                      Grid.Row="1"
                      HorizontalAlignment="Stretch">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Grid.RowSpan="2"
                    Grid.Column="1"
                    Width="100"
                    VerticalAlignment="Stretch"
                    Click="OnAddPhase"
                    Content="Add"
                    FontSize="20" />
        </Grid>
        <Button Grid.Row="1"
                HorizontalAlignment="Stretch"
                Click="OnReset"
                Content="Reset"
                FontSize="20" />
        <Button Grid.Row="1"
                Grid.Column="1"
                HorizontalAlignment="Stretch"
                Click="OnRunReport"
                Content="Run Report"
                FontSize="20" />
        <controls:DataGrid x:Name="ReportView"
                           Grid.Row="3"
                           Grid.ColumnSpan="2"
                           AutoGenerateColumns="False">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Binding="{Binding Phase}" Header="Phase" />
                <controls:DataGridTextColumn Binding="{Binding TotalCustomers}" Header="Total Customers" />
                <controls:DataGridTextColumn Binding="{Binding TotalLoad}" Header="Total Load" />
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</UserControl>