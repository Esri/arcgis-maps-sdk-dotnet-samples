<UserControl x:Class="ArcGISMapsSDK.WinUI.Samples.DisplayFeatureLayers.DisplayFeatureLayers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:esriUI="using:Esri.ArcGISRuntime.UI.Controls">
    <Grid>
        <esriUI:MapView x:Name="MyMapView" />
        <Border MaxWidth="250" Style="{StaticResource BorderStyle}">
            <StackPanel>
                <TextBlock Text="Select a feature layer source" />
                <ComboBox x:Name="FeatureLayerCombo"
                          Width="180"
                          SelectionChanged="FeatureLayerCombo_SelectionChanged" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>