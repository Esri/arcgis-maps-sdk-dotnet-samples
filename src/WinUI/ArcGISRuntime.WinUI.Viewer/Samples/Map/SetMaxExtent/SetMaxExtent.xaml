<UserControl x:Class="ArcGISRuntime.WinUI.Samples.SetMaxExtent.SetMaxExtent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:esriUI="using:Esri.ArcGISRuntime.UI.Controls">
    <Grid>
        <esriUI:MapView x:Name="MyMapView" />
        <Border MaxWidth="220" Style="{StaticResource BorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <CheckBox x:Name="MaxExtentCheckBox"
                          Grid.Column="0"
                          VerticalAlignment="Center"
                          VerticalContentAlignment="Center"
                          Checked="MaxExtentCheckBoxChanged"
                          Unchecked="MaxExtentCheckBoxChanged">
                    <TextBlock Grid.Column="1" Text="Max Extent Enabled" />
                </CheckBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>