<UserControl x:Class="ArcGISMapsSDK.WinUI.Samples.SetMaxExtent.SetMaxExtent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:esriUI="using:Esri.ArcGISRuntime.UI.Controls">
    <Grid>
        <esriUI:MapView x:Name="MyMapView" />
        <Border MaxWidth="200" Style="{StaticResource BorderStyle}">
            <CheckBox x:Name="MaxExtentCheckBox"
                      VerticalAlignment="Center"
                      VerticalContentAlignment="Center"
                      Checked="MaxExtentCheckBoxChanged"
                      Unchecked="MaxExtentCheckBoxChanged">
                <TextBlock Text="Max Extent Enabled" />
            </CheckBox>
        </Border>
    </Grid>
</UserControl>