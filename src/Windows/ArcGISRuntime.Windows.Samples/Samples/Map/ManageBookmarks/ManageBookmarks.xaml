<UserControl 
    x:Class="ArcGISRuntime.Windows.Samples.ManageBookmarks.ManageBookmarks"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:esri="using:Esri.ArcGISRuntime"
    xmlns:esriUI="using:Esri.ArcGISRuntime.UI" 
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <esriUI:MapView x:Name="MyMapView" />

        <Border
                Background="White" BorderBrush="Black" BorderThickness="1"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="30" Padding="20" Width="375">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Bookmarks" Margin="0,0,0,2" TextWrapping="Wrap" />
                    <Button x:Name="ButtonAddBookmark" Content="Add" Click="ButtonAddBookmark_Click" Margin="200,0,0,5"/>
                </StackPanel>
                <ComboBox x:Name="bookmarkChooser" SelectionChanged="OnBookmarkChooserSelectionChanged" />
            </StackPanel>
        </Border>

        <Border Background="AntiqueWhite" BorderBrush="Black" BorderThickness="1"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="30,130" Padding="10" Width="375" x:Name="BorderAddBookmark">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Enter Bookmark Name: " />
                    <TextBox x:Name="TextBoxBookmarkName" Text="" Width="180" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="ButtonAddDone" Content="OK" Margin="0,10,10,0" Click="ButtonAddDone_Click"/>
                    <Button x:Name="ButtonCanel" Content="Cancel" Margin="0,10,10,0" Click="ButtonCanel_Click"/>
                </StackPanel>
            </StackPanel>

        </Border>
    </Grid>
</UserControl>

