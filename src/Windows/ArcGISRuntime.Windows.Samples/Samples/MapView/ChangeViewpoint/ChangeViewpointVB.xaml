<UserControl 
    x:Class="ArcGISRuntime.Windows.Samples.ChangeViewpoint.ChangeViewpointVB"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:esri="using:Esri.ArcGISRuntime"
    xmlns:esriUI="using:Esri.ArcGISRuntime.UI"
    xmlns:mapping="using:Esri.ArcGISRuntime.Mapping" 
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <esriUI:MapView x:Name="MyMapView">
            <mapping:Map>
                <mapping:ArcGISTiledLayer Name="Basemap" 
                    Source="http://services.arcgisonline.com/ArcGIS/rest/services/World_Topo_Map/MapServer"/>
            </mapping:Map>
        </esriUI:MapView>
        <Border Name="uiPanel" 
                Background="White" BorderBrush="Black" BorderThickness="1"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="30" Padding="20" Width="Auto">
            <StackPanel>
                <TextBlock Text="Select one of the button to runt the related Viewpoint code" 
                           Margin="0,0,0,2" TextWrapping="Wrap" />
                <RelativePanel Grid.Row="1" Grid.Column="4" HorizontalAlignment="Center">
                    <Button x:Name="GeomButton"
                        Content="Geometry"
		                Margin="10,10,0,0"
		                Width="Auto"
		                Click="OnGeometryButtonClick"
                        Grid.Column="0"/>
                    <Button x:Name="ScaleButton"
                        Content="Centre and Scale"
		                Margin="10,10,0,0"
		                Width="Auto"
		                Click="OnCentreScaleButtonClick"
                        Grid.Column="1"
                        RelativePanel.RightOf="GeomButton"/>
                    <Button x:Name="AnimateButton"
                        Content="Animation"
		                Margin="10,10,0,0"
		                Width="Auto"
		                Click="OnAnimateButtonClick"
                        Grid.Column="2"
                        RelativePanel.RightOf="ScaleButton"/>
                    <Button x:Name="RotateButton"
                        Content="Rotate"
		                Margin="10,10,0,0"
		                Width="Auto"
		                Click="OnRotateButtonClick"
                        RelativePanel.RightOf="AnimateButton"/>
                </RelativePanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
