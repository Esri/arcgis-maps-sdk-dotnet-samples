<UserControl 
    x:Class="ArcGISRuntime.Windows.Samples.MapRotation.MapRotation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:esriUI="using:Esri.ArcGISRuntime.UI"
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <esriUI:MapView x:Name="MyMapView"/>

        <Border Background="White" Margin="20" Padding="20" VerticalAlignment="Top" HorizontalAlignment="Right" Width="320">
            <RelativePanel HorizontalAlignment="Stretch">
                <TextBlock x:Name="degreeHeader" Style="{ThemeResource TitleTextBlockStyle}" Text="Degrees"/>
                <TextBlock x:Name="degreesValue" Text="{Binding ElementName=degreeSlider, Path=Value}" Style="{ThemeResource TitleTextBlockStyle}" 
                           RelativePanel.RightOf="degreeHeader" Margin="10,0"/>
                <Slider x:Name="degreeSlider" Minimum="0" Maximum="359" ValueChanged="OnDegreeSliderChange" 
                        RelativePanel.Below="degreeHeader" Width="280" />
            </RelativePanel>
        </Border>
    </Grid>
</UserControl>



